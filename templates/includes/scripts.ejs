<script>
  // 固定侧边栏footer
  var sidebar = document.getElementsByClassName('sidebar').item(0);
  var header = sidebar.getElementsByClassName('site-header').item(0);
  var footer = sidebar.getElementsByClassName('site-footer').item(0);
  if (header.clientHeight + footer.clientHeight < sidebar.clientHeight) {
    footer.classList.add('fix');
  }
  // 侧边栏开关
  var handler = document.getElementById('drawer-handler');
  handler.addEventListener('click', function () {
    sidebar.classList.toggle('show');
  });
  var mainContent = document.getElementsByClassName('main-content').item(0);
  mainContent.addEventListener('click', function () {
    if (sidebar.classList.contains('show')) {
      sidebar.classList.remove('show');
    }
  });
</script>
<% if (site.customConfig.mtaSid) { %>
<script>
  var _mtac = {"performanceMonitor":1,"senseQuery":1};
  (function() {
    var mta = document.createElement('script');
    mta.src = '//pingjs.qq.com/h5/stats.js?v2.0.4';
    mta.setAttribute('name', 'MTAH5');
    mta.setAttribute('sid', '<%- site.customConfig.mtaSid %>');
    <% if (site.customConfig.mtaCid) { %>mta.setAttribute('cid', '<%- site.customConfig.mtaCid %>');<% } %>
    var s = document.getElementsByTagName('script')[0];
    s.parentNode.insertBefore(mta, s);
  })();
</script>
<% } %>
<% if (site.customConfig.googleAnalytics) { %>
<script async src="https://www.googletagmanager.com/gtag/js?id=<%- site.customConfig.googleAnalytics %>"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', '<%- site.customConfig.googleAnalytics %>');
</script>
<% } %>
